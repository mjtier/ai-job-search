# lever-cli

CLI for the `lever-search` skill. See `../SKILL.md` for usage and `../url-reference.md` for the API contract.

```bash
bun install      # dev types only; zero runtime dependencies
bun run typecheck
bun run test
```
